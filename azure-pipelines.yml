# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

pool:
  vmImage: 'Ubuntu-16.04'

trigger:
  - master

pr:
  - 'azure-pipelines'

steps:
- script: 'dotnet restore ./demo-repo/Demo.MVC.sln'
- script: 'dotnet build ./demo-repo/Demo.MVC.sln'
